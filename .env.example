# Profile: dev (local small model) | prod (8B + LoRA)
MODEL_PROFILE=dev

# Optional overrides (leave blank to use sensible defaults per profile)
# BASE_MODEL=
# ADAPTER_PATH=
MODEL_CACHE=./hf_models/cache

# Quantization: auto | none | 4bit
QUANTIZATION=auto

# Generation params
MAX_NEW_TOKENS=256
TEMPERATURE=0.7
TOP_P=0.9

# Logging
LOG_LEVEL=info

# Hugging Face auth token if using gated/private models
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxx